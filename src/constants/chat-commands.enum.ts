/**
 * Bot commands user can trigger via chat.
 */
export enum ChatCommands {
    COUNT_MONEY = 'count_money',
    CLEAN_MESSAGES = 'clean_messages',
    SEND_SUMMARY_MESSAGE = 'send_summary_message',
}