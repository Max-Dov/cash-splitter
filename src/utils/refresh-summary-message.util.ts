import {Context} from 'grammy';
import {Party} from '@models';

/**
 * Updates summary message with relevant info.
 */
export const refreshSummaryMessage = (ctx: Context, party: Party): Promise<void> | never => {

}